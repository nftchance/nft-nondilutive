const { assert } = require('chai')

const chai = require('chai')
    .use(require('chai-as-promised'))
    .should()

const { ethers } = require("hardhat")

describe('Non-Dilutive 721 Token', () => { 
    it('Initialize the first base layer of metadata', async () => { 
      
    })  

    it('Handle the setup and minting of the test tokens.', async () => { 

    })
})